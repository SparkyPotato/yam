pub import lang_items.{
    u8, u16, u32, u64, u128,
    i8, i16, i32, i64, i128,
    bool,
    char,
    f32, f64
};

extern "C" fn puts(s: *char) -> i32;

struct ABC {
    a: u64,
    b: u64
}

extern "C" fn main() -> i32 {
    let a = "0123456789";
    let abc = ABC(1, 2);
    puts(a + abc.b);
    0
}
