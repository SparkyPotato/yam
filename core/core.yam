pub import lang_items.{
    u8, u16, u32, u64, u128,
    i8, i16, i32, i64, i128,
    bool,
    char,
    f32, f64
};

extern "C" fn puts(s: *char) -> i32;

struct ABC {
    a: u8,
    b: u16,
    c: u32,
}

extern "C" fn main() -> i32 {
    puts("hai");
    0
}
