fn strlen(s: *const u8) -> usize {
    var curr = s;
    while curr != 0 {
        curr += 1;
    }
    curr - s
}

fn main(argc: i32, argv: *const *const u8) -> i32 {
    if argc == 1 {
        0
    } else {
        strlen(*(argv + 1))
    }
}
