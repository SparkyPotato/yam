[package]
name = "yamc"
version = "0.1.0"
edition = "2021"

[dependencies]
# cfg = { path = "cfg" }
diag = { path = "diag" }
hir = { path = "hir" }
parse = { path = "parse" }
tyck = { path = "tyck" }
# codegen = { path = "codegen" }

clap = { version = "3.1", features = ["derive"] }

[workspace]
members = [
#    "cfg",
#    "codegen",
    "diag",
    "hir",
    "id",
    "parse",
    "tyck",
]

[profile.release]
debug = true

[profile.dev.package.parse]
opt-level = 3
